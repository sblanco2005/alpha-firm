# Copy to mobile/.env on the VPS (.env is git-ignored). Metro inlines EXPO_PUBLIC_* vars.
# API over the Tailscale HTTPS URL:
EXPO_PUBLIC_API_BASE=https://srv1377405.tail5e1256.ts.net
# Must match the dashboard's API_TOKEN (in dashboard/.env.dashboard):
EXPO_PUBLIC_API_TOKEN=replace-with-your-API_TOKEN
